{
  "name": "mobilsayt",
  "version": "1.0.0",
  "description": "Alış-satış platforması - Web, Android və iOS",
  "scripts": {
    "dev:web": "cd web && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "install:all": "npm install && cd web && npm install && cd ../mobil && npm install && cd ../backend && npm install",
    "build:frontend": "cd web && npm install && npm run build && cd ../mobil && npm install && npm run build",
    "build:backend": "cd backend && npm install && npm run build",
    "build": "npm run build:frontend && npm run build:backend",
    "start": "cd backend && npm run start:prod"
  },
  "keywords": ["ecommerce", "marketplace", "react", "android", "kotlin"],
  "author": "",
  "license": "MIT"
}

